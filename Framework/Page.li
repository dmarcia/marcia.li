(use 'Utility/Html')

(deval Copyright (Footer (Link '/copyright' '© dmarcia 2022')))

(defun Page [map content] 
  (Joins 
    [ DocType
      (Html 'en' (Joins 
        [ (Head (Joins 
            [ (Title (Join (Key 'title' map) ' | marcia.li'))
              (Stylesheet '/style.css')
            ]))
          (Body 
            (Joins 
              [ (H1 (Key 'header' map)) 
                content Copyright
              ]))
        ]))
    ]))